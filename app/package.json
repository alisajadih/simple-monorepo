{
  "name": "@soltan/app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn run -T vite",
    "build": "yarn run -T tsc && yarn run -T vite build"
  },
  "dependencies": {
    "@soltan/components": "workspace:^",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
